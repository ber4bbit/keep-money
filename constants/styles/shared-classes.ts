import {StyleSheet} from "react-native";

export const SharedClasses = StyleSheet.create({
    centeredWrapper: {
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center',
    },
    titleText: {
        fontSize: 18,
        fontWeight: 'bold'
    }
})